rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 단일 유저 구조: users/default_user/ddays/{ddayId}
    match /users/default_user/ddays/{ddayId} {
      allow read, write: if true;
    }
  }
}
